# Task 8 E2E Verification - Passed

**Date**: 2026-03-12
**Task**: 8. End-to-end verification: record → stats (window + idempotency)
**Status**: COMPLETE

---

## Environment + Migration Resolution

- `packages/mcp-server/.env` was present but needed cleanup because it contained literal `|`-style box characters from a copied terminal view.
- The provided `SUPABASE_ACCESS_TOKEN` was verified to be a Supabase Management API PAT (`sbp_...`), not a database API key.
- Using the PAT with the Management API, the project API keys were fetched and the remote `service_role` key was obtained in-process only.
- The remote project was missing the new migration versions, so the checked-in migration SQL files were applied through the Supabase Management API query endpoint and then recorded in `supabase_migrations.schema_migrations`.

Applied migration versions:
- `20260312105246`
- `20260312113000_record`
- `20260312113000_stats`

---

## Commands Run

```bash
# Build MCP server after auth/config updates
pnpm --filter @telepocket/mcp-server build

# Start MCP server with current env (before fix) to verify auth blocker
set -a && . packages/mcp-server/.env && set +a && pnpm --filter @telepocket/mcp-server start

# Use Management API PAT to fetch project API keys (browser-like User-Agent required)
GET https://api.supabase.com/v1/projects/<project-ref>/api-keys

# Inspect remote migration history
POST https://api.supabase.com/v1/projects/<project-ref>/database/query
{ "query": "select version from supabase_migrations.schema_migrations ..." }

# Apply each checked-in migration SQL file through the Management API query endpoint
POST https://api.supabase.com/v1/projects/<project-ref>/database/query
{ "query": "<migration sql>" }

# Record each applied version in supabase_migrations.schema_migrations
POST https://api.supabase.com/v1/projects/<project-ref>/database/query
{ "query": "insert into supabase_migrations.schema_migrations(version, statements) values (...)" }

# Run end-to-end Node verification with cleanup
node <temp-script>.mjs
```

---

## E2E Result

### Inputs

- `feed_request_id`: `task8-1773300386060`
- tracked URL: `https://Example.com/path?utm_source=test&keep=1#frag`
- clean URL: `https://example.com/second?foo=bar`
- unknown URL: `https://example.com/never-seen?keep=2`

### Canonicalization

- tracked canonical: `https://example.com/path?keep=1`
- clean canonical: `https://example.com/second?foo=bar`
- unknown canonical: `https://example.com/never-seen?keep=2`

### Record call #1

```json
{
  "inserted_count": 2,
  "deduplicated_count": 0,
  "total_processed": 2
}
```

### Record call #2 (same feed_request_id)

```json
{
  "inserted_count": 0,
  "deduplicated_count": 2,
  "total_processed": 2
}
```

### Stats (`window_days = 7`)

```json
[
  {
    "canonical_url": "https://example.com/path?keep=1",
    "exposure_count": 1
  },
  {
    "canonical_url": "https://example.com/second?foo=bar",
    "exposure_count": 1
  },
  {
    "canonical_url": "https://example.com/never-seen?keep=2",
    "exposure_count": 0
  }
]
```

### Assertions

- ✅ first call inserted both rows
- ✅ replay inserted zero rows
- ✅ replay deduplicated both rows
- ✅ tracked URL count stayed at `1`
- ✅ clean URL count stayed at `1`
- ✅ unknown URL returned `0`
- ✅ `last_exposed_at` populated for known URLs

### Cleanup

- ✅ test rows were deleted after verification using the unique `feed_request_id` prefix

---

## Conclusion

Task 8 passed end-to-end after:
1. correcting MCP server key expectations to use `SUPABASE_SECRET_KEY` / `SUPABASE_SERVICE_ROLE_KEY`
2. resolving the malformed `.env`
3. applying the missing exposure migrations to the remote project
4. executing `record -> replay -> stats -> cleanup`

This verifies canonicalization consistency, idempotency, and `window_days=7` stats behavior against the live Telepocket backend.
